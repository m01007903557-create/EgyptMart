<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# منع إعادة التوجيه
RewriteEngine Off

<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

<FilesMatch "\.(php)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>